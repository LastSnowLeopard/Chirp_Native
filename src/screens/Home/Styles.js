import {COLORS, STYLESHEET} from '../../constants/StyleCommon';

export default STYLESHEET.create({
  continer: {
    flex: 1,
    backgroundColor: COLORS.WHITE,
  },
  innerContiner: {
    flex: 1,
    backgroundColor: COLORS.LIGHTBACKGROUNDCOLOR,
  },
});
